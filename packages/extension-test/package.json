{
  "name": "extension-test",
  "version": "1.0.0-dev",
  "scripts": {
    "dev": "tsc -b -w",
    "build": "tsc -b",
    "e2e": "rimraf dist tsconfig.tsbuildinfo && npm run build && node dist/extensionTestMain"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.16",
    "@types/vscode": "^1.64.0",
    "fs-extra": "^10.0.0",
    "mocha": "^9.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5",
    "vscode-test": "^1.6.1"
  }
}
