{
  "name": "extension-test",
  "version": "1.0.0-dev",
  "scripts": {
    "dev": "tsc -b -w",
    "build": "tsc -b",
    "e2e": "rimraf dist tsconfig.tsbuildinfo && npm run build && node dist/extensionTestMain"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.2",
    "@types/mocha": "^8.0.3",
    "@types/node": "^14.11.10",
    "@types/vscode": "^1.50.0",
    "fs-extra": "^9.0.1",
    "mocha": "^8.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.3",
    "vscode-test": "^1.4.0"
  }
}
