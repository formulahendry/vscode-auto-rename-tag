{
  "name": "extension-test",
  "version": "1.0.0-dev",
  "scripts": {
    "dev": "tsc -b -w",
    "build": "tsc -b",
    "e2e": "rimraf dist tsconfig.tsbuildinfo && npm run build && node dist/extensionTestMain"
  },
  "devDependencies": {
    "@types/fs-extra": "^8.0.1",
    "@types/mocha": "^7.0.1",
    "@types/node": "^13.7.1",
    "@types/vscode": "^1.42.0",
    "fs-extra": "^8.1.0",
    "mocha": "^7.0.1",
    "rimraf": "^3.0.2",
    "typescript": "^3.7.5",
    "vscode-test": "^1.3.0"
  }
}
